# Network Topology Design Simulator (VS Code / Python)

## Setup
1. Open folder in VS Code  
2. Run terminal:
   ```bash
   python3 -m venv venv
   source venv/bin/activate
   pip install -r requirements.txt
Run the simulator:
python main.py